node_modules/
*.sqlite
*.sqlite-shm
*.sqlite-wal
.env
.env.*
!.env.example

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Local dashboard design reviews and screenshots.
/.lavish/
